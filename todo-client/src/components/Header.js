//This file is used for adding the header to the application
import "./Header.scss";

const Header = () => {
  return (
    <header className="todo-header">
      <h1>To-Do Application</h1>
    </header>
  );
};

export default Header;
